services:
  app:
    build:
      context: ../
      dockerfile: ./docker/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - DB_URI=mongodb://root:example@mongo:27017/mydatabase?authSource=admin
      - PORT=3000
      - HOST=localhost